<main class="dadri-page">
  <p class="kicker">Dadri Forecast</p>
  <h1>About</h1>
  <p>
    Dadri Forecast is a community-rooted research and cultural practice shaped in response to land loss, ecological damage,
    caste violence, and extractive development.
  </p>
  <p>
    Unlike the Khandera Art Space about page, this section stays focused on Dadri-specific field conditions and the methods
    of militant, situated research.
  </p>
</main>

<style>
  .dadri-page {
    width: min(900px, 95vw);
    margin: clamp(1rem, 3.5vw, 2.5rem) auto;
    color: var(--text);
    border-top: 2px solid color-mix(in srgb, var(--accent) 64%, var(--line));
    border-bottom: 2px solid color-mix(in srgb, var(--line) 88%, transparent);
    padding: clamp(1rem, 2.4vw, 1.8rem);
    background: color-mix(in srgb, var(--surface) 86%, transparent);
  }

  .kicker {
    margin: 0;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--muted);
    font-size: 0.74rem;
    font-weight: 700;
  }

  h1 {
    margin: 0.45rem 0 0.75rem;
    font-size: clamp(1.8rem, 7vw, 4.2rem);
    letter-spacing: 0.015em;
  }

  p {
    margin: 0 0 0.8rem;
    line-height: 1.75;
    color: color-mix(in srgb, var(--text) 92%, transparent);
  }
</style>
